# Allow Prisma + esbuild native install scripts (Vercel builds need them).
# npm 11+ blocks install scripts by default which breaks `prisma generate`.
allow-scripts=@prisma/client @prisma/engines prisma esbuild