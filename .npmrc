# Allow Prisma + esbuild native install scripts (Vercel builds need them).
# npm 11+ blocks install scripts by default, which would break `prisma generate`.
# Generated by `npm approve-scripts` — the canonical way to record approvals.
allow-scripts[]=prisma
allow-scripts[]=@prisma/client
allow-scripts[]=@prisma/engines
allow-scripts[]=esbuild